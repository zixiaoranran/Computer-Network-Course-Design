Task1 TCP Socket programming 1目的 (1) 理解并掌握TCP socket程序设计的基本结构及工作过程。 (2) 掌握构造应用层自定义的报文结构、报文交互。 2描述 以下即是对总体工作过程的描述，也包含待完成的功能点，请仔细阅 读。 (1) client和server都是基于TCP的命令行程序。client运行在hostos，server运行在guest os。 (2)client发送一个全英文可打印字符的ASCII文件(这个文件你要 自己构造)。 a.cient每次发送这个文件“不定长的一段”给server，由server把这块文本reverse (例如,&quot;a little monkey&quot;变成&quot;yeknom elttil a”)，并返回给client，client在终端打印显示。 b.最终client要输出一个文本文件，该文件是原始文件的全部反转。(3) client和server之间的交互如图1: a.程序运行时，须自动生成一份运行日志文件rm 1ogtxt，记录每一次报文的 发送与接收事件 b.提交时，run_log.txt中的时间戳须与wireshark抓包截图中对应报文的时间戳能够相互印证。验收时会对照二者进行核查。 (4)client和server之间共4种类型文，Initialization、agree、reverseRequest、reverseAnswer。各文封装如图2所示: (5)对图2的文字描述:四种报文类型，通过Type字段来区分。 client 端向 server 端发送 Initialization文，告知 server 做reverse的块数N，server 回复agree文。client逐次向server发送reverseRequest报文，包含了要做reverse的数据;server 端返回reverseAnswer 报文，其中包含了reverse之后的数据。reverseRequest、reverseAnswer报文中Length字段分别指的是Data和 reverseData 的长度，单位是Byte。 client端在程序启动时，在命令行中指定serverlP、serverPort。(6)(7) client每次发送的reverseRequest中的长度都不一样(即不是例如每次固定发送500字节)，长度限定在一个[Lmin，Lmax]之间，最后一块除外。 Lmin、Lmax在命令行中指b.在开始发送数据块前，要先随机确定待发送File中的各块的Bytes长度，再计算块数N，才能执行&quot;client-&gt;server的Initialization报文。 验收时，会给出一组具体的参数(文件内容、Lmin、Lmax chunk_ seed)，要求你能够现场口算或手推出块数N以及第1块、第2块的长度分别是多少。这不需要你背代码，但需要你理解自己代码中分块逻辑的实现方式。例如:“你的文件是520字节，Lmin=50，Lmax=100,eed=42，random依次生成了73、91、58....那你的N是多少?第3块的起始字节是哪里?”。请确保你能清晰描述分块算法，并在代码中指出对应函数的位置 每次client收到来自server的reverseAnswer文，都在命令行下打印出来“第x块:反转的文本”。如“8:yeknomelttila。(9) server能够同时处理2个及以上client 的同时请求。 Task2 UDP socket programming 建议使用python。 要学习一下python的多线程 1目的 (1) 掌握UDPsocket程序结构。 (2) 掌握自定义应用层报文格式及报文交互过程，即自定义应用层协议 2描述 以下即是对总体工作过程的描述，也包含待完成的功能点，请仔细阅 读。 (1) client和server都是基于UDP的命令行程序。client运行在hostos，server运行在guest os。client端启动时在命令行方式下指定serverlP serverPort。即，不是在代码中把IP、portno固定死，可以传参给程序再run。 (2)在UDP socket程序中模拟TCP的“连接建立”过程。 即建立完毕连接以后，才进入到“数据传输”阶段。这就需要你去构造client和server之间交互数据的首部，在首部中定义用于连接建立的字段。 大家已经学过，UDP是非面向连接的，即UDP socket程序中，client 端是直接sendto数据给 server，见P104页的UDPclient.py程序。 c.对使用TCP的socket应用程序而言，应用层程序不用担心它所发送或接收的数据的可靠 性问题。因为TCP去搞定丢失损坏等场景 d.如果应用程序即要使用UDP socket还要'可靠性。那这种可靠性’就得应用程序的开发者(在应用层)来实现 连接建立报文的首部中(不管你咋设计)，必须包含一个StudemD字段(2字节，无符号 型)。其值的计算规则为:取本人学号后4位数字，与0x5A3C做XOR运算，结果 入该字段。server端在收到连接请求时，须验证该字段是否符合规则(即对收到的值再次XOR0x5A3C，检查结果是否是合法的4位整数范围0-9999);若不合法，server拒绝连接并打印错误信息 (3) 在“数据传输”阶段，要模拟TCP的可靠数据传输。对client发送的数据包，server端采用随机丢弃(随机不响应)的方式，“假装没收到”，来模拟丢包场景，丢包率自定。 解释说明 你现在在写基于UDP的应用层程序，即你的程序依赖UDP。UDP虽然不可靠，但指望UDP发生丟包事件也是概率极低的，即 server端的运输层UDP没收到client端运输层的UDP数据报。所以“丢包”这种事，你得自己在应用层程序中来模拟、制造场景。 b.你可以不模拟损坏’的场景。如果你想实现，那当然越完善越好。 程序运行时，须自动生运行日志文件rm_ 10g.txt，记录每一次发包、收包、超时、重传事件。提交时，r log.txt中的时间戳须与wireshark抓包截图中对应数据包的时间戳能够相互印证(误差在合理范围内)。验收时会对照二者进行核查。日志文件须为程序生成，不得手动编车 只考虑client==&gt;server 发送数据时的不可靠即可。假设server==&gt;client 的完全没问题。(4) 给client设定一个超时时间，假设是300ms，你可以自己调整这个 值。在超时时间内 a.若能接收到来自于server的响应报文，则计算本次交互的RTT(单位:ms)以及sever的系统时间(hh-mm-ss)。 b.server可以对client发送的多个报文采用累积确认方式的响应 报文 c.若client没有收到来自于server的回复，则认为发了丢包。若发生丢包，则重传。 对“超时时间设定为300ms这事”，更精确的做法是，不断收集往返RTT，去实时计算、 调整超时时间。大家学过TCP估算超时时间的公式，见教材P155。本实验中，你可以自定义更简单计算，例如把若干次RTT的平均值的5倍作为超时时间，等等，你可以自己 如果你模拟了‘快重传’(教材P160)，那更好了，但不做必须完成的要求。 (5)client端，应用层采用固定“发送窗口”大小400字节。 解释说明:模拟GBN协议。client端的发送窗口固定为400字节，即发送端窗口最大400字节。每次发送的UDP数据包中的数据大小限定在40字节到80字节之间，每次随机(课堂上讲解GBN时，是把每个data都按固定长度来对待的，还记得?)。窗口内容纳最多10个、最少5个UDP数据包。server作为接收端，每次只能接收1个UDP数据包。 出于简化目的，你也可以每次固定发送80字节，最多一次发5个80字节的udp数据包，假设分别是1、2、3、4、5。只有对5个中的第1个udp数据包被确认了，才可以继续发送第6个数居包 进一步，如果你想模拟SR协议，那当然更好啦。但不做必须完成的要求 (6)client端每发送一个udp数据包，print出“第n个(第x-y字节)client端已经发送”，n是你发送的数据包的编号、x和y是你发送过的数据的编号。 a.若收到了来自于server端的response，则print出“第n个(第X~y字节)server端已经收到，RTT是xxxms” b.若发生超时，假设300ms(这个数字可以自定义)内没有收到来自于server 的response，发生重传，则print出“重传第n个(第x~y字节)数据包”。 第xy字节)”，说明你得跟踪记录数每段数据的边界注意:这里要求的“第n个( (7) 根据上面各条的要求，你自定义这个可以(在一定程度上)保证可靠性的UDP socket 应用层的协议首部的格式。 注意:每个人对协议格式即首部的设计，从命名到字段大小到顺序，完全重合的概率是极低的。如果两或多人完全一样，那极大概率是互相抄袭。 (8)当你想发送给server的30个(这个数字你可以自己定)UDP数据包，server都收到以后。在client端，打印出如下[汇总】信息:丢包率:xx%。按“30-实际发送的udp packet number&quot;计 算。 整个过程中的最大RTT、最小RTT、平均RTT、RTT的标准差。(统计量等核算，请使用python的pandas。pandas是你今后必学的。) (9) 在client-server 交互过程中，使用 wireshark抓包